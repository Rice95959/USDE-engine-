services:
  - type: web
    name: usde-clarity-engine
    runtime: docker
    plan: free
    envVars:
      - key: MISTRAL_API_KEY
        sync: false
      - key: MISTRAL_MODEL
        value: mistral-large-latest
    healthCheckPath: /api/health
